<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ReflectionDerping;assembly=ReflectionDerping"
        MinWidth="500" MinHeight="300">
  <Window.DataContext>
    <local:BasicViewModel/>
  </Window.DataContext>
  <StackPanel>
    <TextBlock>Hello world!</TextBlock>
    <!--<CheckBox IsChecked="{Binding Path=IsChecked}" IsThreeState="True"/>-->
    <!--<TextBlock Text="{Binding IsChecked, Converter={StyleResource NullableValueConverter}}"/>-->
    <TextBlock Text="{Binding Path=X}"/>
  </StackPanel>
</Window>